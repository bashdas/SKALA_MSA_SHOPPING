spring:
  application:
    name: user-service
  datasource:
    url: jdbc:h2:mem:userdb-test
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: create-drop

jwt:
  secret: test-only-jwt-secret-key-with-more-than-thirty-two-bytes-2026
  access-token-expiration-seconds: 3600
